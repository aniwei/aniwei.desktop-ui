import React from 'react'
import ReactDom from 'react-dom'

export default class About extends React.Component {
  render () {
    const { dataSource } = this.props

    return <div className="view__app-about">
      about
    </div>
  }
}
