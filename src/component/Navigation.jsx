import React from 'react'
import ReactDom from 'react-dom'

export default class Navigation extends React.Component {
  render () {
    const { dataSource } = this.props

    return <div className="view__app-tabbar">
    </div>
  }
}
